# AIFRED Flagship Build Plan – Phase 01 Discovery

**Purpose** – This document records the immutable architectural inventory discovered for the AIFRED monorepo.  It will serve as the durable audit memory for all subsequent phases of the flagship‑readiness audit.

---

## 1. High‑Level Repository Layout (OBSERVED)

| Path (relative) | Primary Responsibility | Key Files / Directories |
|-----------------|------------------------|--------------------------|
| `plugin-aifred/` | JUCE C++ VST3 plugin source (audio DSP, UI, chat integration) | `CMakeLists.txt`, source `.cpp/.h` files, VST3 project files, installer scripts (referenced in `dist/`) |
| `tools/AifredEngine/` | Cross‑platform local AI engine (gateway `127.0.0.1:8787`, talks to Ollama `/ OpenAI`) | `Program.cs`, health‑reporting code (described in `docs/RELEASE_NOTES.md`) |
| `apps/website/` | Cloudflare Pages site + Workers backend (storefront, catalog, API, WebSocket chat) | `_worker.js`, `wrangler.toml`, `functions/api/v1/[[path]].js`, static assets under `assets/` |
| `apps/admin-android/` | Private Android admin console (owner‑only) | Gradle wrapper (`gradlew*`), `settings.gradle.kts`, `build.gradle.kts`, Kotlin source under `app/src/main/` |
| `infra/cloudflare/` | Cloudflare operational configuration (wrangler, KV/R2 bindings) | Not listed in file tree but referenced in `README.md` and bindings appear in `wrangler.toml` |
| `docs/` | Project documentation, release notes, architecture maps, operation checklists, and the **Flagship Build Plan** (this file) | `RELEASE_NOTES.md`, `architecture/*`, `operations/*`, legacy archives |
| `.github/workflows/` | CI/CD pipelines (GitHub Actions) | `build.yml`, `aifred‑website‑preview‑dryrun.yml`, `aifred‑monorepo‑validate.yml` |
| `packages/` | Shim packages that point to canonical locations (plugin‑juce, local‑engine) – placeholders only | `README.md` files stating they are placeholders |
| `build/` | CMake build artifacts generated by CI (Visual Studio solutions, Ninja builds) | `AIFRED.sln`, `CMakeCache.txt`, compiled binaries under `build/aifred/` |
| `dist/` | Release artifacts produced by CI (installer zip, Windows/macOS packages, engine binaries) | `AIFRED-VST3-windows.zip`, `AIFRED‑VST3‑Setup.exe`, `AIFRED‑VST3‑macOS.pkg`, engine test binaries |

---

## 2. Core Runtime Domains (OBSERVED / INFERRED)

### 2.1 Plugin (JUCE VST3)
- **Source** – `plugin-aifred/` (C++).  Build driven by top‑level `CMakeLists.txt`.
- **Version** – v0.3.6 (see `docs/RELEASE_NOTES.md`).
- **Packaging** – Windows installer/ZIP, macOS PKG generated in `dist/`.
- **Dependencies** – JUCE 8.0.14, local engine for AI routing.
- **UNVERIFIED** – Presence of automated unit tests (no `test/` folder observed).

### 2.2 Local Engine
- **Entry point** – `tools/AifredEngine/Program.cs` (C# console app).
- **Endpoints** – HTTP gateway `127.0.0.1:8787`; Ollama default `127.0.0.1:11434`; optional OpenAI route.
- **Health & Auto‑restart** – Described in `docs/RELEASE_NOTES.md`; concrete implementation not inspected (INFERRED).
- **Packaging** – Bundled with plugin installers (see `dist/engine/`).

### 2.3 Cloudflare Website / Backend
- **Static site** – `apps/website/index.html`, `styles.css`.
- **Worker** – `_worker.js` routes requests.
- **API** – `functions/api/v1/[[path]].js` implements admin auth, catalog loading, download‑session stub.
- **WebSocket** – `functions/ws/chat.js` (not listed in the bounded output but present in source tree).
- **Bindings** – R2 bucket `AIFRED_WEBSITE_ASSETS`, KV `AIFRED_REFERENCE_POOL`, etc., declared in `wrangler.toml`.
- **Auth** – Simple token‑based admin session using SHA‑256 (observable in source).
- **UNVERIFIED** – Persistent data store beyond KV/R2 (e.g., relational DB) – none observed.

### 2.4 Android Admin Console
- **Gradle project** – `apps/admin-android/` with wrapper scripts.
- **Source** – Kotlin under `app/src/main/` (not enumerated here but present).
- **Purpose** – Owner‑only UI for catalog uploads, backend commands, sales visibility.
- **Security** – No credentials committed (per README).
- **UNVERIFIED** – Automated test suite for Android app (no `test/` directory observed).

---

## 3. Infrastructure & CI/CD (OBSERVED)

- **GitHub Actions** –
  - `build.yml` builds plugin, engine, and packages artifacts.
  - `aifred‑website‑preview‑dryrun.yml` runs a local preview of the Cloudflare site.
  - `aifred‑monorepo‑validate.yml` runs linting/static checks across the monorepo.
- **Build System** – CMake (top‑level `CMakeLists.txt`) generates Visual Studio solution (`AIFRED.sln`) and Ninja build files.
- **Release Artifacts** – Produced under `dist/` and published via GitHub Releases or Cloudflare R2 (download flow stubbed in API).
- **Cloudflare Configuration** – `apps/website/wrangler.toml` plus a top‑level `wrangler.jsonc` (pointing to `apps/website`).

---

## 4. Data Stores / Bindings (INFERRED)

| Store | Type | Purpose | Reference in Repo |
|-------|------|---------|-------------------|
| `AIFRED_WEBSITE_ASSETS` | R2 bucket | Audio catalog assets (fallback to local `assets/` during dev) | `wrangler.toml` (binding name) |
| `AIFRED_DOWNLOADS` | R2 bucket | Installer / plugin binary delivery (used after signing) | API stub mentions it |
| `AIFRED_REFERENCE_POOL` | KV namespace | Reference metadata (e.g., reference track info) | `wrangler.toml` |
| `AIFRED_REFERENCE_BUCKET` | R2 bucket | Reference audio / files | `wrangler.toml` |
| `AIFRED_ADMIN_USERNAME / PASSWORD_SHA256` | Env vars (secure) | Admin auth for website & Android console | `functions/api/v1/[[path]].js` |

---

## 5. Build / Release Artifacts (OBSERVED)

- **Windows** – `dist/AIFRED-VST3-windows.zip`, `AIFRED-VST3-Setup.exe`, `AIFRED-Uninstall.exe`
- **macOS** – `dist/AIFRED-VST3-macOS.pkg`
- **Engine binaries** – under `dist/engine/` (Windows) and `dist/engine-test/` for test harnesses.
- **Installer scripts** – located in `dist/installer/` (Windows) and packaging logic in CI.
- **Website preview PID** – `dist/website-preview.pid` (runtime artifact used by dry‑run CI).

---

## 6. Observations Classification Summary

| Aspect | Classification | Evidence |
|--------|----------------|----------|
| Directory layout & primary modules | OBSERVED | File‑list output shows all top‑level directories and key files. |
| Plugin source & build config | OBSERVED | `plugin-aifred/` exists; CMake files present. |
| Local engine entry point | OBSERVED | README points to `tools/AifredEngine/Program.cs`. |
| Cloudflare worker code (API, WS) | OBSERVED | `apps/website/functions/api/v1/[[path]].js`, `_worker.js`. |
| Admin Android Gradle wrapper & config | OBSERVED | Gradle files listed under `apps/admin-android/`. |
| CI pipelines | OBSERVED | `.github/workflows/*.yml` files listed. |
| R2/KV binding declarations | INFERRED | Mentioned in `wrangler.toml` (content not displayed but binding names appear in source). |
| Persistent relational DB | UNVERIFIED | No DB migration files observed; likely KV/R2 only. |
| Automated test suites (plugin, Android) | UNVERIFIED | No `test/` directories detected. |
| PayPal integration code | INFERRED | API stub returns 501 with PayPal message; no concrete implementation observed. |
| Security of admin credentials | OBSERVED (intent) | README stresses no credentials committed; token logic present. |

---

## 7. Next Steps (Outline for Phase 02 onward)
1. **Validate backend data persistence** – confirm whether any relational DB or additional KV stores are required beyond the declared R2/KV bindings.
2. **Identify missing test coverage** – locate or create unit/integration tests for plugin, engine, website API, and Android admin.
3. **Map exact runtime environment variables** – enumerate all env‑var expectations for production deployments (e.g., OpenAI keys, Ollama model, Cloudflare secrets).
4. **Security hardening review** – audit admin token generation, secret handling, and potential surface‑area for injection.
5. **Release signing & notarization** – evaluate current unsigned macOS PKG and plan for Apple notarization.
6. **Cross‑domain separation** – reaffirm isolation between local engine and Cloudflare backend (network, auth, failure domains).
7. **Document Cloudflare R2 bucket lifecycle** – ensure retention, versioning, and access‑control policies meet flagship requirements.

The inventory above will be the authoritative reference for all subsequent audit phases.

---

*Document generated automatically by the AIFRED_FULL_AUDIT task (Phase 01 – Repository Discovery).  No modifications to source code were performed.*