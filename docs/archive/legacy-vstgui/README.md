# Legacy VSTGUI Reference

This archive note refers to the sibling repository:

```text
../aifred-plugin
```

It is not the canonical runtime plugin for Phase 1. That repository is an older VSTGUI/Steinberg SDK plugin line with different AI routing and package assumptions.

Potentially useful later:

- VSTGUI implementation ideas.
- Older installer notes.
- Historical release notes and metric docs.
- UI/UX experiments that can be reviewed before any future migration.

Do not delete the sibling repo yet. Do not copy it wholesale into the monorepo during Phase 1. Do not revive its direct Ollama `/v1` route unless explicitly approved by a later architecture decision.
