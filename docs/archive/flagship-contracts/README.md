# Flagship Contracts Reference

This archive note refers to the sibling repository:

```text
../AIFRED_Official-
```

It is not the canonical runtime source for Phase 1. That repository is a future/flagship clean-room planning workspace with contracts, Python truth-layer work, bridge experiments, and explicit warnings that plugin, backend, website, and admin implementation are not active runtime code yet.

Potentially useful later:

- Architecture contracts.
- Mode and truthfulness rules.
- Python truth-layer tests and interface ideas.
- Bridge and response-validation concepts.

Do not delete the sibling repo yet. Do not treat its skeleton app/plugin/backend folders as deployed product code. Migrate only reviewed contracts or specific implementation pieces in a later phase.
