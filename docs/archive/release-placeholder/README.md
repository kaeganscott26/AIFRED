# Release Placeholder Reference

This archive note refers to the sibling repository:

```text
../aifred-downloads
```

It is not the canonical release asset source for Phase 1. The repository currently acts as a placeholder/reference and does not contain the live release artifact set in this checkout.

Potentially useful later:

- A historical marker for public download intent.
- A place to compare against Cloudflare R2 naming if needed.

Do not delete the sibling repo yet. Do not assume it contains production installers. Current release authority remains the `AIFRED` release workflow plus the website backend's R2/GitHub fallback logic.
