AIFRED System Requirements

Windows runtime: 64-bit Windows 10 or newer, a VST3 host such as FL Studio, and enough local disk space for the plugin plus optional Ollama setup.

Optional local AI: Ollama for the default out-of-box local setup path.

Website backend: Cloudflare Pages Functions for website API routes. OpenAI is configured through environment variables when cloud mode is used.
