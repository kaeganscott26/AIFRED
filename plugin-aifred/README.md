# Beta frontend

The native frontend uses the [shared core](../shared-dsp/README.md). Build, install, test and state behavior are documented in the [product README](../README.md). No separate frontend analyzer or model serializer exists.
